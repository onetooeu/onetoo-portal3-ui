.wrangler/
node_modules/
.DS_Store

# local signing artifacts (must never ship)
agent.key
agent.pub
sig.bin
canonical.txt
body.json

# local/dev secrets & probes
.dev.vars
__probe_root.txt
portal/__probe_portal.txt
portal/.well-known/_write_test.txt

# local scratch
audit/
public/
